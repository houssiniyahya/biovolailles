CREATE TABLE `environment_measurement_records` (
	`id` text PRIMARY KEY NOT NULL,
	`building_id` text NOT NULL,
	`lot_id` text,
	`measurement_type` text NOT NULL,
	`value` real NOT NULL,
	`unit` text NOT NULL,
	`occurred_at` text NOT NULL,
	`source_type` text NOT NULL,
	`source_id` text,
	`actor_id` text,
	`data_status` text NOT NULL,
	`measurement_method` text,
	`device_id` text,
	`document_id` text,
	`validation_status` text,
	`created_at` text NOT NULL,
	FOREIGN KEY (`building_id`) REFERENCES `buildings`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`lot_id`) REFERENCES `lots`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`actor_id`) REFERENCES `users`(`id`) ON UPDATE no action ON DELETE no action
);
--> statement-breakpoint
CREATE TABLE `feed_usage_records` (
	`id` text PRIMARY KEY NOT NULL,
	`lot_id` text NOT NULL,
	`occurred_at` text NOT NULL,
	`quantity` real NOT NULL,
	`unit` text NOT NULL,
	`feed_type` text NOT NULL,
	`feed_source` text,
	`source_type` text NOT NULL,
	`source_id` text,
	`actor_id` text,
	`data_status` text NOT NULL,
	`measurement_method` text,
	`device_id` text,
	`document_id` text,
	`validation_status` text,
	`created_at` text NOT NULL,
	FOREIGN KEY (`lot_id`) REFERENCES `lots`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`actor_id`) REFERENCES `users`(`id`) ON UPDATE no action ON DELETE no action
);
--> statement-breakpoint
CREATE TABLE `mortality_records` (
	`id` text PRIMARY KEY NOT NULL,
	`lot_id` text NOT NULL,
	`occurred_at` text NOT NULL,
	`count` integer NOT NULL,
	`suspected_cause` text,
	`cause_validated` integer DEFAULT false NOT NULL,
	`source_type` text NOT NULL,
	`source_id` text,
	`actor_id` text,
	`data_status` text NOT NULL,
	`measurement_method` text,
	`device_id` text,
	`document_id` text,
	`validation_status` text,
	`created_at` text NOT NULL,
	FOREIGN KEY (`lot_id`) REFERENCES `lots`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`actor_id`) REFERENCES `users`(`id`) ON UPDATE no action ON DELETE no action
);
--> statement-breakpoint
CREATE TABLE `water_usage_records` (
	`id` text PRIMARY KEY NOT NULL,
	`building_id` text NOT NULL,
	`lot_id` text,
	`occurred_at` text NOT NULL,
	`quantity` real NOT NULL,
	`unit` text NOT NULL,
	`source` text,
	`source_type` text NOT NULL,
	`source_id` text,
	`actor_id` text,
	`data_status` text NOT NULL,
	`measurement_method` text,
	`device_id` text,
	`document_id` text,
	`validation_status` text,
	`created_at` text NOT NULL,
	FOREIGN KEY (`building_id`) REFERENCES `buildings`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`lot_id`) REFERENCES `lots`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`actor_id`) REFERENCES `users`(`id`) ON UPDATE no action ON DELETE no action
);
--> statement-breakpoint
CREATE TABLE `weight_measurement_records` (
	`id` text PRIMARY KEY NOT NULL,
	`lot_id` text NOT NULL,
	`occurred_at` text NOT NULL,
	`average_weight` real NOT NULL,
	`unit` text NOT NULL,
	`sample_count` integer,
	`source_type` text NOT NULL,
	`source_id` text,
	`actor_id` text,
	`data_status` text NOT NULL,
	`measurement_method` text,
	`device_id` text,
	`document_id` text,
	`validation_status` text,
	`created_at` text NOT NULL,
	FOREIGN KEY (`lot_id`) REFERENCES `lots`(`id`) ON UPDATE no action ON DELETE no action,
	FOREIGN KEY (`actor_id`) REFERENCES `users`(`id`) ON UPDATE no action ON DELETE no action
);
